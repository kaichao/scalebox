# 1. Scalebox编程模型

串行可编程控制器驱动的分布式执行引擎

```
                          +-----------------------+
                          |   Control Plane       |
                          |-----------------------|
                          | - 全局数据库 database  |
                          | - 调度管理 controld    |
                          | - 执行槽启动 actuator  |
                          +----------+------------+
                                     |
                   +-------------------------------------+
                   |                 |                   |
            +------------+    +------------+      +------------+
            | node-agent |    | node-agent |      | node-agent |
            +------+-----+    +------+-----+      +------+-----+
                   |                 |                   |
           +-------------+    +-------------+    +--------------+
           |   slot 1    |    |   slot 1    |    |   slot 1     |
           |   slot 2    |    |   slot 2    |    |   slot 2     |
           |   slot N    |    |   slot N    |    |   slot N     |
           +-------------+    +-------------+    +--------------+
                    |
              Module Pipeline
        (moduleA → moduleB → moduleC)

```

## 1.1 事件驱动编程范式

支持事件驱动式结构。

- sidecar模式：
- 天然分布式设计

分层：
- 算法模块层：多种程序语言实现
- 模块连接层：shell编程，算法执行结果反馈到系统（后续消息路径、执行时间、I/O数据、执行结果（stdout/stderr））
- 并行程序全局算法间层：用yaml定义的模块间关联。

{画一个示例图}



模块间的应用逻辑表达：基于任务分发，实现控制流
    在并行程序中，每个模块（算法模块、传输模块、控制模块等）相当于操作原语    



## 1.2 核心概念：App、Module、Task、Host、Slot

| 概念     | 英文名称   | 中文名称 | 说明                                                     |
| :------ | :--------- | :------ | :------------------------------------------------------ |
| 应用程序 | **App**    | **应用** | 完成特定计算任务的应用程序，包含多个Module。App可通过调用资源调度系统（如Slurm）动态获取所需的计算资源。 |
| 软件单元 | **Module** | **模块** | 构成应用程序App的基本可编程单元，是以容器化封装的程序代码集合。通常为一个独立算法组件，或跨节点间传输的组件等。组件间通过级联形成数据处理流水线。|
| 执行实例 | **Task**   | **任务** | 基本运行单位，输入数据在具体Module上的执行过程。 |
| 计算节点 | **Host**   | **节点** | 执行计算任务的服务器节点。                                 |
| 资源单元 | **Slot**   | **插槽** | 节点上与Module对应的计算资源切片，是Task细粒度运行调度的基础单位  |


## 1.3 运行时环境（Runtime）

- controld
- actuator
- database
- cluster-admin ：与HPC调度系统交互的资源申请、管理的软件模块。也是一个系统级应用。


## 1.4 并行化策略：模块内、模块级、模块间并行

- 并行方式
  - 模块级数据并行
  - 模块间流水线并行
  - 模块内代码并行

