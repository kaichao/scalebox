name: remote-primes
cluster: cluster0
parameters:
  main_router: main
  initial_status: INITIAL

modules:
  main:
    arguments:
      code_path: ${PWD}/main-code
      slot_options: slot_on_head
    environments:
      - NUM_GROUPS=${NUM_GROUPS}
    sink_vmodules:
      - calc
