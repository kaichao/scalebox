# GROUP-BOUND vtask：vtask 绑定节点组（head slot 锚定节点组）
CLUSTER=inline
VTASK_MODE=GROUP-BOUND
# TASK_DIST_MODE 仅驱动 vtask-head 模块的平台参数（实现层枚举，勿改）
TASK_DIST_MODE=SLOT-BOUND
HEAD_SLOTS=h0:2
CORE_MODE=HOST-BOUND
CORE_SLOTS=n[01]-[01]
