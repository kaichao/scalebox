version: '1.0.0'

label: Global Definition Cluster bio-down

clusters:
  local:
    parameters:
      base_dir: /gfs
      base_data_dir: /gfs/mydata
      uname: ${USER}
    memo: local cluster
    hosts:
      h0:
        label: head node
        ip_addr: ${LOCAL_ADDR}
        role: head
