version: '1.0.0'

local:
  parameters:
    base_data_dir: /shared/mydata
    uname: ${USER}
    port: 22
    local_ip_index: 1
    comments: local cluster
  hosts:
    h0:
      label: head node
      ip_addr: ${LOCAL_ADDR}
