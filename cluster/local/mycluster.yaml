version: '1.0.0'

label: Global Definition Cluster bio-down

clusters:
  local:
    base_dir: /gfs
    base_data_dir: /gfs/mydata
    # base_log_dir: /gfs/mylog
    uname: ${USER}
    memo: local cluster
    hosts:
      h0:
        label: head node
        ip_addr: ${LOCAL_ADDR}
        role: head
