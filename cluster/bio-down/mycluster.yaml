version: '1.0.0'

label: Global Definition Cluster bio-down

specs:
  bio-vm-00:
    # CPU cores
    num_cores: 4
    # Memory(GB)
    mem_gb: 16.0
    # Disk(GB)
    disk: 200.0

clusters:
  bio-down:
    parameters:
      base_dir: /gfs
      base_data_dir: /gfs/mydata
      uid: root
    memo: bio-down cluster
    hosts:
      h0:
        label: head node
        ip_addr: 10.0.6.100
        role: head
        spec: bio-vm-00
        memo: CentOS 8
      n0:
        ip_addr: 10.0.6.101
        spec: bio-vm-00
      n1:
        ip_addr: 10.0.6.102
        spec: bio-vm-00
      n2:
        ip_addr: 10.0.6.103
        spec: bio-vm-00
      n3:
        ip_addr: 10.0.6.104
        spec: bio-vm-00
