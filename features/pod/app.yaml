name: pod.features
cluster: ${CLUSTER}
parameters:
  main_router: router
modules:
  mod-1:
    parameters:
      task_dist_mode: HOST-BOUND
      pod_id: my_pod
    slots: 
      - n0-[01]
  mod-2:
    parameters:
      task_dist_mode: HOST-BOUND
      pod_id: my_pod
    slots: 
      - n0-[01]
  router:
    arguments:
      code_path: ${PWD}/router-code
      slot_options: slot_on_head
  