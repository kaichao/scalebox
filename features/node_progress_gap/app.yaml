name: node_progress_gap.features
cluster: ${CLUSTER}

modules:
  node-progress-gap:
    parameters:
      task_dist_mode: HOST-BOUND
      node_progress_gap: '{"n-[01]":2,"n":3}'
    slots: 
      - n-[01][01]
  