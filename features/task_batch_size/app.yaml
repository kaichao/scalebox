name: task_batch_size.features
cluster: ${CLUSTER}

modules:
  task-batch-size:
    base_image: hub.cstcloud.cn/scalebox/agent:latest
    arguments:
      slot_options: slot_on_head
    parameters:
      task_batch_size: '${TASK_BATCH_SIZE}'
